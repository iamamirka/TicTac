<resources>
    <style name="Theme.TicTac" parent="android:Theme.Material.NoActionBar">
        <!-- Without this the window starts dark grey and flashes before Compose draws. -->
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
